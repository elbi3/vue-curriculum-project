In Vue 3 Composition API, a composable is essentially the equivalent concept to a React custom hook.

Store your ref objects inside the function so that wherever you call the composable, that component has access to the composable's state.

[mdn on window.innerWidth](https://developer.mozilla.org/en-US/docs/Web/API/Window/innerHeight)